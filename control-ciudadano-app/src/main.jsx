import React from "react";
import ReactDOM from "react-dom/client";
import ControlCiudadano from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ControlCiudadano />
  </React.StrictMode>
);
